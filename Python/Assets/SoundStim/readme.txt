This folder is for sound stimuli.
Each type of sound stimuli should have its own folder and be added to the "SoundTypes" option in the cfg.json file in the config folder.
the result should look like:
{..., "SoundTypes": ["TypeOneFolderName", "TypeTwoFolderName"], ...}
